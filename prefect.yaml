# Generic metadata about this project
name: repo-prefect
prefect-version: 3.4.9.dev3

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning this project in remote locations

pull:
  - prefect.deployments.steps.github_clone:
      id: clone-step
      # Gunakan repository_url sesuai hasil inspect
      repository: "{{ prefect.blocks.github-repository.github-repo.repository_url }}"
      # Gunakan access_token dengan memanggil token di dalam credentials
      access_token: "{{ prefect.blocks.github-credentials.github-credential.token }}"